server:
  port: ${PORT:${SERVER_PORT:8181}}

spring:
  application:
    name: gateway
  zipkin:
    baseUrl: http://localhost:9411/
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: mscatalog
          uri: lb://api-catalog
          predicates:
            - Path=/api/v1/catalog/**
        - id: msmovie
          uri: lb://api-movie
          predicates:
            - Path=/api/v1/movies/**

info:
  app:
    name: ${spring.application.name}

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: true
    serviceURL:
      defaultZone: http://localhost:8761/eureka
management:
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: gateway